<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Camera preview -->
    <androidx.camera.view.PreviewView
        android:id="@+id/authPreviewView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"
        app:implementationMode="performance"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Drawing overlay for boxes and labels -->
    <com.example.scanner_sdk.customview.single.OverlayView
        android:id="@+id/authOverlayView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"
        android:importantForAccessibility="no"
        app:layout_constraintTop_toTopOf="@id/authPreviewView"
        app:layout_constraintBottom_toBottomOf="@id/authPreviewView"
        app:layout_constraintStart_toStartOf="@id/authPreviewView"
        app:layout_constraintEnd_toEndOf="@id/authPreviewView"
        tools:ignore="MissingClass" />

    <!-- Top Header -->
    <LinearLayout
        android:id="@+id/auth_top_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end"
        android:padding="16dp"
        android:background="@android:color/transparent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        >

        <ImageButton
            android:id="@+id/auth_btn_flash_toggle"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@drawable/round_bg"
            android:src="@drawable/ic_flash_off"
            android:contentDescription="Gallery"
            android:scaleType="centerInside"
            android:padding="8dp"
            app:tint="@color/white" />

        <ImageButton
            android:id="@+id/auth_btn_camera_switch"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@drawable/round_bg"
            android:layout_marginHorizontal="12dp"
            android:src="@drawable/ic_camera_switch"
            android:contentDescription="Gallery"
            android:scaleType="centerInside"
            android:padding="8dp"
            app:tint="@color/white" />

        <ImageButton
            android:id="@+id/auth_btn_gallery"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@drawable/round_bg"
            android:src="@drawable/ic_gallery"
            android:contentDescription="Gallery"
            android:scaleType="centerInside"
            android:padding="8dp"
            app:tint="@color/white" />

        <ImageButton
            android:id="@+id/auth_btn_history"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@drawable/round_bg"
            android:src="@drawable/ic_history"
            android:layout_marginHorizontal="12dp"
            android:contentDescription="Gallery"
            android:scaleType="centerInside"
            android:padding="8dp"
            app:tint="@color/white" />

        <ImageButton
            android:id="@+id/auth_btn_settings"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@drawable/round_bg"
            android:src="@drawable/ic_settings"
            android:contentDescription="Gallery"
            android:scaleType="centerInside"
            android:padding="8dp"
            app:tint="@color/white" />
    </LinearLayout>

    <!-- Camera Controls -->
    <LinearLayout
        android:id="@+id/auth_bottom_controls"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:padding="16dp"
        android:background="@android:color/transparent"
        android:layout_marginBottom="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <SeekBar
            android:id="@+id/auth_zoom_seekbar"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:max="100"
            android:progress="0"
            android:progressTint="@android:color/white"
            android:thumbTint="@android:color/white" />

    </LinearLayout>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginEnd="6dp"
        android:padding="4dp"
        android:background="@drawable/rounded_bg"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        >
        <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_add"
            app:tint="@color/white"
            android:padding="8dp"/>
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="1.0X"
            android:textColor="@color/white"
            android:layout_marginVertical="6dp"
            />
        <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_minus"
            app:tint="@color/white"
            android:padding="8dp"/>
    </LinearLayout>

    <!-- 🔲 Scanner Frame Overlay -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/auth_scanner_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Scanner Frame (90% width, centered square) -->
        <FrameLayout
            android:id="@+id/auth_scanner_square"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintWidth_percent="0.8"
            app:layout_constraintDimensionRatio="1:1"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:background="@android:color/transparent">

            <!-- Top Left Corner -->
            <View
                android:layout_width="40dp"
                android:layout_height="4dp"
                android:background="@android:color/white"
                android:layout_gravity="top|start" />

            <View
                android:layout_width="4dp"
                android:layout_height="40dp"
                android:background="@android:color/white"
                android:layout_gravity="top|start" />

            <!-- Top Right Corner -->
            <View
                android:layout_width="40dp"
                android:layout_height="4dp"
                android:background="@android:color/white"
                android:layout_gravity="top|end" />

            <View
                android:layout_width="4dp"
                android:layout_height="40dp"
                android:background="@android:color/white"
                android:layout_gravity="top|end" />

            <!-- Bottom Left Corner -->
            <View
                android:layout_width="40dp"
                android:layout_height="4dp"
                android:background="@android:color/white"
                android:layout_gravity="bottom|start" />

            <View
                android:layout_width="4dp"
                android:layout_height="40dp"
                android:background="@android:color/white"
                android:layout_gravity="bottom|start" />

            <!-- Bottom Right Corner -->
            <View
                android:layout_width="40dp"
                android:layout_height="4dp"
                android:background="@android:color/white"
                android:layout_gravity="bottom|end" />

            <View
                android:layout_width="4dp"
                android:layout_height="40dp"
                android:background="@android:color/white"
                android:layout_gravity="bottom|end" />

        </FrameLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.constraintlayout.widget.ConstraintLayout>